framework:
    router:
        # Výchozí adresa pro generování URL mimo HTTP (cron/CLI). Reálnou adresu
        # instance (z UI/DB) nastavuje za běhu RouterContextConsoleSubscriber.
        default_uri: 'http://localhost'

when@prod:
    framework:
        router:
            strict_requirements: null
